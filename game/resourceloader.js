var resourceFolder = "game/resources/";
var loadedTextures = 0;
var totalTextures = 3;

var ResourceLoader = new Class({
    initalize: function() {
        alert("hoi ik besta");
        //this.textureGrass;
        //this.textureStone;
        //this.textureWood;
        //loadTexture(textureGrass, url);
        //loadTexture(texture, url);
        //loadTexture(texture, url);
    }
});

/*
function loadTexture(texture, url) {
    texture = new Texture(url);
}

function onLoadTexture(texture) {
    loadedTextures++;
    alert("Loadedtextures now is "+loadedTextures);
}
*/